from django.urls import path # Необходимые импорты
from . import views # из папки в которой находится данный файл (а именно urls.py) будет испортирован файл views.py

urlpatterns = [ # Переменная urlpatterns содержит в себе список функций path. Функция path - "Путь/след" отвечает за то какая функция представления будет отрабаотывать по какому url путм, принимает в себя имя url адреса, путь к функции представления и иные необязательные аргументы, например name
    path('', views.p0, name='portfolio0'), # первая функция path. Содержит адрес в виде пустой строки (" "). Следовательно пользователь введя portfolio автоматически пападает на эту страницу. Так называемая главная страница приложения (путь https:porfolio/ Появляется portfolio т.к. данный файл уже второй, первый был файл urls.py из папки my_site, у котром и было по адресу portfolio перенаправлено всё дальше на этот файл). Вторым аргументом передан путь к функции views.p0 файла views.py (views.views.p0). Третим аргументом задан аргумент name, определяющий имя url адреса, по которому на него можно будет ссылаться в шаблонах. В данном случае 'portfolio0'
    path('p1/', views.p1,  name='portfolio1') # вторая функция path. Содержит адрес в виде строки 'p1'. Следовательно пользователь введя portfolio/p1 пападает на эту страницу. Вторым аргументом передан путь к функции views.p1 файла views.py (views.views.p1). Третим аргументом задан аргумент name, определяющий имя url адреса, по которому на него можно будет ссылаться в шаблонах. В данном случае 'portfolio1'
]
